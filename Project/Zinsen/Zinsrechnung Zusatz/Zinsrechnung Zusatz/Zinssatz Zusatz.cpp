#include <iostream>

using namespace std;

void main()
{
	float Wert1, Wert2, Wert3, max, min;

	cout << "Geben sie ihren ersten Wert an:" << endl;
	cin >> Wert1;
	cout << "Geben sie ihren zweiten Wert an:" << endl;
	cin >> Wert2;
	cout << "Geben sie ihren dritten Wert an:" << endl;
	cin >> Wert3;

	max = Wert1
	min = Wert2

	if (Wert1 > Wert2)
	{
		max = Wert1
		min = Wert2
	}
	if (Wert3 > max)
	{
		max = Wert3
	}	
	if (Wert2 > max)
	{
		max = Wert2
	}
	cout << "Ihre Maximaler Wert ist:"<< max << endl << "Ihr Kleinster Wert ist"<< min << endl;
}